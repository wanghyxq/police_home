# 同步历史预约列表样式

根据您的要求，我们将把 `pages/barber/history.vue` 的列表样式更新为与 `pages/barber/dashboard.vue` 一致的三行式布局。

## 1. 样式调整 (`pages/barber/history.vue`)

我们将替换 `v-for` 循环内部的模板代码。

### 新的模板结构

```html
<view v-for="(item, index) in historyList" :key="index" @click="goDetail(item.id)"
  class="bg-white p-4 rounded-xl shadow-sm border border-gray-100 flex justify-between items-center active:bg-gray-50 cursor-pointer">
  <view>
    <!-- 第一行：时间 -->
    <view class="font-mono text-xl font-bold text-police mb-1">{{ item.appointTime }}</view>
    <!-- 第二行：日期（带图标） -->
    <view class="text-xs text-gray-500 flex items-center">
        <i class="fa-regular fa-calendar mr-1"></i>{{ item.appointDate }}
    </view>
    <!-- 第三行：手机号 -->
    <view class="text-xs text-gray-500 mt-1">
        {{ item.userPhone }}
    </view>
  </view>
  <view class="text-right flex items-center">
    <view class="text-xs mr-2">
      <text v-if="item.status === '0' || item.status === 'booked'" class="text-green-600 font-bold">待服务</text>
      <text v-else-if="item.status === '1' || item.status === 'pending_eval'" class="text-orange-500">待评价</text>
      <text v-else class="text-gray-400">已完成</text>
    </view>
    <i class="fa-solid fa-chevron-right text-gray-300 text-xs"></i>
  </view>
</view>
```

**注意字段映射**:

* `item.time` -> `item.appointTime`

* `item.date` -> `item.appointDate`

* `item.userPhone` -> `item.userPhone` (保持不变)

## 2. 实施步骤

1. **修改** **`pages/barber/history.vue`**：使用 `SearchReplace` 工具替换列表渲染部分的模板代码。

